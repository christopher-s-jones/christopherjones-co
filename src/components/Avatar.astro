---
import Image from "astro/components/Image.astro"
const { imageSrc, imageAlt, imageWidth, imageHeight, imageStyles, figureStyles } = Astro.props;
---
    <figure class={figureStyles}>
    <Image
    src={imageSrc}
    widths={[192, 256, 512]}
    sizes="(max-width: 320px) 192px, (max-width: 640px) 256px, (max-width: 1024px) 512px"
    class={imageStyles}
    alt={imageAlt}
    width={imageWidth}
    height={imageHeight}
    />
</figure>